# v1.6 Release Spec

Release readiness requires a clean `develop/v1.6.0` branch descended from `v1.5.0`, locked roadmap conformance, TypeScript/build/architecture QA, targeted and global regressions, teacher runtime E2E, source immutability, fail-closed behavior, and no unapproved scope.
