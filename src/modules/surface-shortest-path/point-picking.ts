import type { Vector3 } from "three";
import type { CanonicalSurfacePoint } from "./types.js";

export function polyhedralIntersectionToSurfacePoint(input:{id:string;solidId:string;faceId:string;triangleVertexIds:[string,string,string];barycentric:[number,number,number];worldPoint:Vector3;incidentSurfaceIds?:string[]}):CanonicalSurfacePoint{if(input.barycentric.some(value=>!Number.isFinite(value)||value<0)||Math.abs(input.barycentric.reduce((a,b)=>a+b,0)-1)>1e-6)throw new Error("INVALID_SURFACE_POINT");const positive=input.barycentric.filter(value=>value>1e-7).length,attachmentType=positive===1?"VERTEX":positive===2?"EDGE":"FACE";return{id:input.id,solidId:input.solidId,surfaceId:input.faceId,attachmentType,localCoordinates:{kind:"face_barycentric",vertexIds:input.triangleVertexIds,weights:input.barycentric},incidentSurfaceIds:input.incidentSurfaceIds??[input.faceId],worldCoordinates:[input.worldPoint.x,input.worldPoint.y,input.worldPoint.z],provenance:"user"};}
export function curvedIntersectionToSurfacePoint(input:{id:string;solidId:string;surfaceId:string;theta:number;longitudinal:number;worldPoint:Vector3}):CanonicalSurfacePoint{if(!Number.isFinite(input.theta)||!Number.isFinite(input.longitudinal))throw new Error("INVALID_SURFACE_POINT");return{id:input.id,solidId:input.solidId,surfaceId:input.surfaceId,attachmentType:"CURVED_SURFACE",localCoordinates:{kind:"curved",theta:input.theta,longitudinal:input.longitudinal},worldCoordinates:[input.worldPoint.x,input.worldPoint.y,input.worldPoint.z],provenance:"user"};}
