import type { DocumentIR } from "../document-engine/document-ir.js";
export type SourceDisposition="MAPPED"|"NON_QUESTION"|"UNSUPPORTED"|"UNRESOLVED";
export interface SourceBlockRecord { sourceBlockId:string; sourceDocumentId:string; sourceObjectIds:string[]; sourceAnchor:unknown; structureType:"ANSWER"|"SOLUTION"; detectionEvidence:string[]; disposition:SourceDisposition; provenance:unknown; qaStatus:string; issues:string[] }
const text=(d:DocumentIR)=>d.blocks.map(b=>({b,t:b.content.filter((x:any)=>x.type==="text").map((x:any)=>x.value).join(" ")}));
export function discoverAnswerSolutionSourceBlocks(d:DocumentIR):SourceBlockRecord[]{const out:SourceBlockRecord[]=[];for(const {b,t} of text(d)){const kind=/^(?:đáp\s*án|answer|lời\s*giải|hướng\s*dẫn\s*giải|solution)\b/iu.test(t)?(/^(?:đáp\s*án|answer)\b/iu.test(t)?"ANSWER":"SOLUTION"):undefined;if(!kind)continue;out.push({sourceBlockId:`${d.sourceHash}::${kind.toLowerCase()}::${b.id}`,sourceDocumentId:d.sourceDocumentId??d.sourceHash,sourceObjectIds:[b.id],sourceAnchor:{sourceDocumentId:d.sourceDocumentId??d.sourceHash,partName:"word/document.xml",objectIndex:b.order,paragraphIndex:b.paragraphIndex},structureType:kind,detectionEvidence:["EXPLICIT_HEADING"],disposition:"UNRESOLVED",provenance:d.provenance??{},qaStatus:"REVIEW",issues:[]});}return out;}
export function validateAnswerSolutionMappings(rows:SourceBlockRecord[], mappings:{questionId:string;sourceBlockId:string;sourceAnchor?:unknown;evidence?:string[];provenance?:unknown}[], questionIds:Set<string>){const ids=new Set(rows.map(x=>x.sourceBlockId)),seen=new Set<string>(),errors:string[]=[];for(const m of mappings){const key=`${m.questionId}::${m.sourceBlockId}`;if(!questionIds.has(m.questionId)||!ids.has(m.sourceBlockId))errors.push("BROKEN_SOURCE_REFERENCE");if(seen.has(key))errors.push("DUPLICATE_MAPPING");seen.add(key);if(!m.evidence?.length||!m.provenance)errors.push("MISSING_MAPPING_EVIDENCE");}return {valid:errors.length===0,errors,answerCount:mappings.length};}
