export interface ExpectedPackageManifest { questionId:string; sourceDocumentId:string; sourceObjectIds:string[]; expectedMathReferenceCount:number; expectedAssetReferenceCount:number; expectedTextBlockIds:string[]; requiredProvenanceFields:string[] }
export function buildExpectedPackageManifest(q:any):ExpectedPackageManifest{return {questionId:q.id,sourceDocumentId:q.sourceDocumentId,sourceObjectIds:[...q.sourceObjectIds],expectedMathReferenceCount:q.mathObjectIds.length,expectedAssetReferenceCount:q.assetIds.length,expectedTextBlockIds:q.sourceObjectIds.filter((x:string)=>x.includes("paragraph")),requiredProvenanceFields:["sourceFile","sourceSha256"]};}
export function validatePackageManifest(expected:ExpectedPackageManifest,actual:any){const errors:string[]=[];if(actual.id!==expected.questionId)errors.push("QUESTION_ID");for(const id of expected.sourceObjectIds)if(!actual.sourceObjectIds?.includes(id))errors.push(`SOURCE_OBJECT:${id}`);if((actual.mathObjectIds?.length??0)!==expected.expectedMathReferenceCount)errors.push("MATH_REFERENCE");const actualAssetIds=[...new Set((actual.assets??[]).map((a:any)=>a.assetId))];if(actualAssetIds.length!==expected.expectedAssetReferenceCount)errors.push("ASSET_REFERENCE");for(const f of expected.requiredProvenanceFields)if(!actual.provenance?.[f])errors.push(`PROVENANCE:${f}`);return {valid:errors.length===0,errors};}
