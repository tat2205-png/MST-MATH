# v1.5.0 Known Non-Blocking Warnings

Architecture QA reports three pre-existing warnings and zero errors:

1. VisualTab UI boundary review.
2. IntegrationDrawer UI boundary review.
3. Duplicate Vite dependency declaration warning.

The production build also reports a chunk-size warning. These warnings did not fail release QA and were not changed during release finalization.
