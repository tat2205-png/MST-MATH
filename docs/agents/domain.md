# Domain docs

This repository uses the single-context layout: a root `CONTEXT.md` when present and
architecture decisions under `docs/adr/`. Agents should read relevant domain context
and ADRs before changing the corresponding area.
